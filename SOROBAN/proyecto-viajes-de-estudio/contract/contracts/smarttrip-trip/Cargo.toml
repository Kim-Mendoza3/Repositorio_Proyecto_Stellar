[package]
name = "smarttrip-trip"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.7.3"

[dev-dependencies]
soroban-sdk = { version = "21.7.3", features = ["testutils"] }

[features]
default = []
